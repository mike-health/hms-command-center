# Org chart snapshot — 2026-09-06 pre-editable

Frozen copy of the live org chart **before** HEA-97 editable work.

**Do not delete or overwrite this directory.** Restore from these files if a later phase regresses layout or data.

| File | Source |
|------|--------|
| `org-chart.html` | `public/modules/org-chart.html` |
| `app.css` | `public/css/app.css` |
| `app.js` | `public/js/app.js` |
| `command-center.json` | `data/command-center.json` |

Restore (manual): copy each file back to its source path, then restart the server so `FACTORY_DEFAULTS` reloads from `command-center.json`.
